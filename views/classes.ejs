<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Classes - Het Huiswerk-Portaal</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
  <nav class="navbar">
    <div class="nav-content">
      <h1>🎓 Het Huiswerk-Portaal</h1>
      <div class="nav-right">
        <span class="user-info">
          <strong><%= user.name %></strong>
          <span style="opacity: 0.7;"> • <%= user.role.charAt(0).toUpperCase() + user.role.slice(1) %></span>
        </span>
        <a href="/logout" class="btn btn-secondary">Logout</a>
      </div>
    </div>
  </nav>
  
  <div class="container main-content">
    <div class="classes-header">
      <h2>📚 Your Classes</h2>
      <p>Choose a class to view or upload homework assignments</p>
    </div>
    
    <div class="classes-grid">
      <% classes.forEach((className, index) => { 
        const icons = ['📘', '📗', '📙', '📕', '📓', '📔', '📖'];
        const icon = icons[index % icons.length];
      %>
        <a href="/classes/<%= encodeURIComponent(className) %>" class="class-card">
          <div class="class-icon"><%= icon %></div>
          <h3><%= className %></h3>
          <p>View homework assignments</p>
        </a>
      <% }) %>
    </div>
  </div>
</body>
</html>
