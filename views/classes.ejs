<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Classes - Het Huiswerk-Portaal</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
</head>
<body>
  <nav class="navbar">
    <div class="nav-content">
      <h1>Het Huiswerk-Portaal</h1>
      <div class="nav-right">
        <span class="user-info">Welcome, <%= user.name %> (<%= user.role %>)</span>
        <a href="/logout" class="btn btn-secondary">Logout</a>
      </div>
    </div>
  </nav>
  
  <div class="container main-content">
    <div class="classes-header">
      <h2>Select a Class</h2>
      <p>Choose a class to view or upload homework</p>
    </div>
    
    <div class="classes-grid">
      <% classes.forEach(className => { %>
        <a href="/classes/<%= encodeURIComponent(className) %>" class="class-card">
          <div class="class-icon">📚</div>
          <h3><%= className %></h3>
          <p>View homework</p>
        </a>
      <% }) %>
    </div>
  </div>
</body>
</html>
